import { ChakraProvider, Heading, AbsoluteCenter } from '@chakra-ui/react';

const Map = () => {
  return (
    <ChakraProvider>
        <Heading color={'black'}>Search!</Heading>
    </ChakraProvider>
  );
};

export default Map;
